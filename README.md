# 文系数学トレーナー v0.2

## 修正内容
- 大文字・小文字、全角・半角、空白、括弧、記号の違いを吸収して採点
- `x=8` と `8`、`x=4,y=3` と `(4,3)` など代表的な表記揺れに対応
- 式の問題は数値代入による数学的同値判定を追加
- 最後の答えをペンで入力できる回答欄を追加
- ペン回答は誤認識を避けるため、模範解答と比較して自己採点
- Service Workerのキャッシュをv0.2へ更新

GitHubでは `index.html` と `sw.js` を差し替えてください。
